# Systemd override configuration for Nginx self-healing
# This ensures Nginx automatically restarts if it crashes
# Prevents infinite restart loops under extreme failure

[Service]
Restart=always
RestartSec=3
StartLimitIntervalSec=60
StartLimitBurst=5
